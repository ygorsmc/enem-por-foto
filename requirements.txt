# Runtime — versões alinhadas ao projeto pai (SDKs já validados em produção).
fastapi==0.111.0
uvicorn==0.30.1
httpx==0.28.1
redis==5.0.4
pydantic==2.13.4
pydantic-settings==2.14.1
structlog==24.1.0
tenacity==8.3.0
mistralai==2.4.12
google-genai==1.75.0
openai==2.44.0
# Só usadas quando QUEUE_BACKEND=azure (deploy scale-to-zero). Importadas de
# forma tardia, então o backend memory (dev/testes) nunca as toca.
azure-storage-queue==12.11.0
aiohttp==3.10.11
